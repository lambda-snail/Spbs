@page "/me/settings/{Area}"

@if (_readyToRender)
{
    <DynamicComponent
        @ref="_dynamicComponent"
        Parameters="_dynamicComponentParameters"
        Type="@(Area is not null && _settingsPageMap.TryGetValue(Area, out var value) ? value : _defaultSettingsPage)"/>
}