@page "/accounts/import-expenses/in-progress"

<MudText Typo="Typo.h4" GutterBottom="true" Class="mt-4">Import Job in Progress</MudText>

<MudPaper Elevation="15"
          Outlined="true">

    @if (_isImportingExpenses)
    {
        <MudText Typo="Typo.body1">Import in progress, please wait ...</MudText>

        <MudProgressLinear Value="@_numExpensesImported" Min="0" Max="@_numExpensesToImport" Class="my-7"/>

        <MudText Typo="Typo.body1">Loaded @_numExpensesImported.ToString() of @_numExpensesToImport.ToString() expenses</MudText>
        @* *@
        @* *@
        @* <div> *@
        @*     <p></p> *@
        @*     <div class="progress"> *@
        @*         <MudProgressLinear Value="17.75" Min="17.0" Max="18.0" Class="my-7"/> *@
        @*         <div class="progress-bar" role="progressbar" style="@($"width: {GetImportProgressLabelPercentage()}%")" aria-valuenow="@_numExpensesImported" aria-valuemin="0" aria-valuemax="@_numExpensesToImport">@($"{GetImportProgressLabelPercentage()}%")</div> *@
        @*     </div> *@
        @*     <div>Loaded @_numExpensesImported.ToString() of @_numExpensesToImport.ToString() expenses</div> *@
        @* </div> *@
    }
    else if (_isImportJobComplete)
    {
        <MudText Typo="Typo.body1">@_numExpensesImported.ToString() expenses have been successfully imported!</MudText>
        <MudText Typo="Typo.body2">
            Go to the <MudLink Href="expenses">expenses page</MudLink> to see and edit the new expenses.
        </MudText>
    }
</MudPaper>